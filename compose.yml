services:
  oracle:
    image: gvenzl/oracle-free:23-slim
    container_name: devarchive-oracle
    ports:
      - "1521:1521"
    environment:
      ORACLE_PASSWORD: oracle
      APP_USER: devarchive
      APP_USER_PASSWORD: devarchive
    volumes:
      - oracle-data:/opt/oracle/oradata
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  oracle-data:
