spring:
  datasource:
    url: ${DB_URL:jdbc:oracle:thin:@localhost:1521/FREEPDB1}
    username: ${DB_USERNAME:devarchive}
    password: ${DB_PASSWORD:devarchive}
    driver-class-name: oracle.jdbc.OracleDriver
  h2:
    console:
      enabled: false
  flyway:
    locations: classpath:db/migration/oracle
